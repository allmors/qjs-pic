{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env QJS_ROOTDIR=functions QJS_PORT=56553 QJS_PREFIX=/api QJS_STATIC=uploads run-qjs --init",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "fast-qjs": "^0.0.4"
  }
}